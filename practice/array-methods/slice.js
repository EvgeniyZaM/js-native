const arr = [1, 2, 3, 4, 5]

const newArr = arr.slice() // копирование элементов из массива arr, в массив newArr
const newArr2 = arr.slice(-2)// [4, 5]
const newArr3 = arr.slice(2) // [3, 4, 5]
const newArr4 = arr.slice(1, -1) // [2, 3, 4]
