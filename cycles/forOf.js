// Можно итерироваться по строке, массиву, set

const str = "hello"
for (let char of str) {
  console.log(char) // Символы
}

const arr = [1, 2, 3, 4, 5]
for (let item of arr) {
  console.log(item) // Элементы
}
